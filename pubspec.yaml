name: chat_messanger_ui
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  scrollable_positioned_list: ^0.1.7
  custom_pop_up_menu: ^1.2.0
  flutter_hooks: ^0.16.0
  hooks_riverpod: ^0.14.0+1
  socket_io_client: ^0.9.12
  provider: ^5.0.0
  intl: ^0.17.0
  flushbar: ^1.10.4
  flutter_svg: ^0.21.0+1
  fluentui_system_icons: 1.1.54
  username_gen: 1.0.2
  google_fonts: 1.0.0

  cupertino_icons: ^0.1.3
  line_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/